# requirements_hf.txt — HuggingFace Spaces (CPU, 12 GB RAM, 16 GB disk)
# Minimal dependency set for low-resource hackathon demo.
# PyTorch CPU wheel is installed separately in Dockerfile.hf BEFORE this file.
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0

# LangChain (core only — no community extras that pull in large deps)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.3.0

# CrewAI (query path bypasses ReAct loop; kept for ingest crew scaffold)
crewai>=0.36.0

# Embeddings — lightweight sentence-transformers backend (bge-small ~130 MB)
sentence-transformers>=3.0.0

# LLM Runtime — llama-cpp-python for GGUF, transformers for embeddings
transformers>=4.40.0
accelerate>=0.34.0
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.3.0

# Vector Database (embedded, no server required)
chromadb>=0.5.0

# Graph Database (embedded)
kuzu>=0.11.3

# Hybrid Search
rank-bm25>=0.2.2

# Document Loaders
PyMuPDF>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
pandas>=2.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
unstructured[pdf,image]>=0.12.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.24.0
pydantic>=2.0.0

# Encryption (for session security key)
cryptography>=41.0.0

# System monitoring (CPU/RAM banner in UI)
psutil>=5.9.0
rank_bm25
spacy>=3.7.0
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
